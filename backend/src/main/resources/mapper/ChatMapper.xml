<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mingle_market.mapper.ChatMapper">

  <insert id="insertChatRoom">
    INSERT INTO chat_rooms (product_id, buyer_id, seller_id)
    VALUES (#{productId}, #{buyerId}, #{sellerId})
  </insert>

  <select id="fintChatRoom" resultType="ChatRoomDto">
    SELECT * FROM chat_rooms
    WHRER product_id = #{productId}
    AND buyer_id = #{buyerId}
    AND seller_id = #{sellerId}
  </select>

  <insert id="insertMessage">
    INSERT INTO messages (room_id, sender_id, text)
    VALUES (#{roomId}, #{senderId}, #{text})
  </insert>

  <select id="getMessages" resultType="ChatDto">
    SELECT * FROM messages
    WHRER room_id = #{roomId}
    ORDER BY created_at ASC
  </select>

  <select id="getChatRooms" resultType="ChatRoomDto">
    SELECT * FROM chat_rooms
    WHERE buyer_id = #{userId} OR seller_id = #{userId}
    ORDER BY created_at DESC
  </select>

</mapper>