<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mingle_market.mapper.ProductMapper">

    <select id="getAllProducts" resultType="ProductDto">
        SELECT * FROM products ORDER BY created_at DESC
    </select>

    <select id="getProductById" resultType="ProductDto">
        SELECT * FROM products WHERE id = #{id}
    </select>

    <insert id="insertProduct">
        INSERT INTO products (title, price, location, image, seller_id)
        VALUES (#{title}, #{price}, #{location}, #{image}, #{sellerId})
    </insert>

    <update id="updateProduct">
        UPDATE products
        SET title = #{title}, price = #{price}, location = #{location}, image = #{image}
        WHERE id = #{id}
    </update>

    <delete id="deleteProduct">
        DELETE FROM products WHERE id = #{id}
    </delete>

</mapper>